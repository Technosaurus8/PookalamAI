name: pookalamai
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.3

dependencies:
  cloud_firestore: 6.2.0
  firebase_core: 4.6.0
  flutter:
    sdk: flutter
  google_fonts: ^8.2.1
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/drawn_pookalam.png
    - assets/original_pookalam.png
